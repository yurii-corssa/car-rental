import CarDropdown from "components/CarDropdown/CarDropdown";
import { FilterContainer } from "./Filter.styled";

const Filter = () => {
  return (
    <FilterContainer>
      <CarDropdown />
    </FilterContainer>
  );
};

export default Filter;
